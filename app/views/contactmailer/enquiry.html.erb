<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h3>Dear <%= @contact.property.name %></h3>

    <h4>MY Person <%= @contact.name %>  has shown interest in <%= @contact.property.title %>  Please contact him on details below:</h4>
    <p>
      Person Name : <%= @contact.name if @contact.name.present? %>
    </p>
    <p>      
      Person Email: <%= @contact.email if @contact.email.present?%>.<br/>
    </p>
    <p> Person PhoneNumber: <%= @contact.phone if @contact.phone.present?%></p>
    <p>
      Message: <%= @contact.question if @contact.question.present? %>
    </p>
    <p>Thanks,</p>
    <p>Ezy Props Team</p>
  </body>
</html>